# Orbia v0.9 Final Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish v0.9 by correcting demonstrated security, storage, migration, integration, and performance-verification gaps without adding major AI features.

**Architecture:** Preserve the existing Main-owned AI routing, SQLite vault storage, persistent jobs, IPC validation, and renderer stores. Harden shared trust boundaries centrally, correct the storage manager against the actual semantic-index schema, and use deterministic benchmark fixtures rather than production rewrites without evidence.

**Tech Stack:** Electron 39, React 19, TypeScript 5.9, Zustand 5, better-sqlite3, Vitest 4, ESLint, electron-vite.

**Spec:** `C:\Users\Dell\.codex\attachments\e08adbbc-c55f-4413-8182-7df09b65ec65\pasted-text.txt`

## Global Constraints

- Do not add major AI features or quizzes.
- Preserve existing v0.8 data and all unrelated dirty-worktree changes.
- `LOCAL_ONLY` must never call cloud providers; cloud fallback must pass the same privacy gate.
- Source navigation remains Main-validated and cannot be authored by model output.
- AI storage cleanup must be category-specific and must never delete original course media.
- Optimize production code only when profiling demonstrates a problem.
- Run fresh full tests, typecheck, lint, and production build before completion.

---

### Task 1: Correct AI storage accounting and migration ownership

**Files:**

- Modify: `src/main/services/ai/ai-storage.service.ts`
- Modify: `src/main/services/ai/ai-usage.service.ts`
- Modify: `src/main/services/database.service.ts`
- Test: `test/hardening-storage-usage.test.ts`
- Test: `test/hardening-migrations.test.ts`

**Interfaces:**

- Consumes: `semantic_index_chunks`, `semantic_index_embeddings`, `ai_local_usage` in the current vault database.
- Produces: accurate `AiStorageStats`, independent cleanup, and migration-owned local usage counters.

- [x] Add a failing storage test that inserts a real semantic chunk and embedding, then proves the category is measured and can be cleared without touching course media records.
- [x] Add a failing migration assertion that `ai_local_usage` exists immediately after connecting a migrated vault.
- [x] Run `npx vitest run test/hardening-storage-usage.test.ts test/hardening-migrations.test.ts` and confirm the failures name the stale table and missing migration.
- [x] Replace stale `semantic_index_vectors.vector_blob` references with `semantic_index_embeddings.vector`; add the idempotent hardening migration and keep runtime table creation only as backward-compatible defense.
- [x] Re-run the two tests and confirm they pass.

### Task 2: Centralize untrusted AI source delimiting

**Files:**

- Modify: `src/main/services/ai/ai-prompts.ts`
- Modify: `src/main/services/chat/grounded-prompt.ts`
- Modify: `src/main/services/summaries/summaries.service.ts`
- Modify: `src/main/services/chapters/chapters.service.ts`
- Modify: `src/main/services/ai-notes/ai-notes.service.ts`
- Test: `test/hardening-security.test.ts`
- Test: `test/summaries.test.ts`
- Test: `test/chapters.test.ts`
- Test: `test/ai-notes.test.ts`

**Interfaces:**

- Produces: `UNTRUSTED_CONTENT_RULES` and `wrapUntrustedContent(label, value, maxCharacters)` for all model-bound user/library content.
- Preserves: existing JSON response schemas and provider routing.

- [x] Extend existing security tests so malicious transcript, PDF, note, and material content must remain escaped inside an explicit untrusted-data boundary.
- [x] Add focused assertions to the existing summary/chapter/note tests that the model receives the shared guard and escaped payload.
- [x] Run the focused tests and confirm current Phase 6 prompts fail those assertions.
- [x] Implement the smallest shared helper and apply it at each content-generation boundary.
- [x] Re-run focused security and Phase 6 tests.

### Task 3: Complete integration and safety audit

**Files:**

- Review/modify only if a failing regression proves necessary: `src/main/ipc/*.ipc.ts`, `src/preload/index.ts`, `src/types/api.ts`, `src/renderer/src/App.tsx`, player/search/chat/summary/chapter/note surfaces.
- Test: existing v0.9 provider, privacy, transcription, semantic-index, retrieval, grounding, search, summary, chapter, note, migration, and renderer-store suites.

**Interfaces:**

- Preserves: renderer sends IDs/intents; Main validates entity ownership and resolves navigation.

- [x] Run all focused v0.9 tests.
- [x] Audit IPC registration/preload/type contracts and UI mount points for every implemented Phase 1–6 surface.
- [x] Audit key handling, logs, cloud routing, cancellation, persistence, and failure isolation using code evidence.
- [x] Remove only demonstrated dead/debug/duplicate code; do not refactor unrelated modules.

### Task 4: Make performance evidence reproducible

**Files:**

- Modify: `test/hardening-performance.test.ts`

**Interfaces:**

- Produces: representative measurements for 100 and 500 courses, thousands of lessons, a large transcript/index, database size, heap delta, and bounded retrieval/search work.

- [x] Reproduce the 50 ms failure under parallel full-suite load and compare it with isolated execution.
- [x] Confirm whether production retrieval performs per-item SQL or bounded bulk reads.
- [x] Replace the flaky micro-timing gate with generous regression ceilings and explicit metrics suitable for isolated execution; do not hide algorithmic regressions.
- [x] Run the benchmark isolated and record its measurements for the final report.

### Task 5: Final v0.9 verification and release metadata

**Files:**

- Modify only if required: `package.json`, lockfile, plan/ledger documentation.

**Interfaces:**

- Produces: a buildable v0.9 snapshot with fresh verification evidence.

- [x] Review the complete tracked and untracked diff against the hardening specification.
- [x] Run `npm test` and record files/tests/failures.
- [x] Run `npm run typecheck`.
- [x] Run `npm run lint`.
- [x] Run `npm run build`.
- [x] Confirm no quiz implementation was added and report limitations without starting v1.0.
